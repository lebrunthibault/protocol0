.turbo
.idea
node_modules/
.logs/
/.env

# Bytecode from the stdlib-only scripts/ tooling
/scripts/__pycache__/

# Artefacts de packaging (installeur Windows)
/build/
/dist-installer/
/src/agent-python/build/
/src/agent-python/dist/

# Front Vue 3 (src/frontend) : build buildé en CI, jamais committé.
/src/frontend/dist/
/src/frontend/node_modules/
# Copie de prebuild du design system (source unique = src/website/design-system.css).
/src/frontend/src/styles/design-system.css

# Lock transient du scheduler de tâches Claude (jamais committé).
/.claude/scheduled_tasks.lock

# Companion icon PNG (visual debugging): regenerated by scripts/windows/generate_icon.py.
# The .ico, on the other hand, is committed (embedded as a PE resource by src/agent/build.rs).
/installer/assets/protocol0.png
