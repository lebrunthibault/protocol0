{
  "name": "protocol0-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Protocol 0 local web UI (homepage + keymapper + API docs), served by the agent on port 9010.",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/copy-design-system.mjs",
    "predev": "node scripts/copy-design-system.mjs",
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "hotkeys-js": "^3.13.7",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
