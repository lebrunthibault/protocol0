{
  "name": "@code/p0_script",
  "private": true,
  "scripts": {
    "format": "black .",
    "lint": "poetry run flake8 . && poetry run mypy . && poetry run vulture .",
    "test": "poetry run pytest -s ."
  },
  "dependencies": {},
  "packageManager": "npm@8.11.0"
}
