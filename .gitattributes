# The PowerShell scripts invoked by the Inno Setup installer must keep their exact
# bytes and not be subjected to EOL conversion or re-normalization by Git. Windows
# PowerShell 5.1 is byte-sensitive about encoding; treat them as binary to be safe.
installer/windows/*.ps1 -text
